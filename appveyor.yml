version: 1.0.{build}
pull_requests:
  do_not_increment_build_number: true
environment:
  nodejs_version: 8
install:
- ps: Install-Product node $env:nodejs_version
- npm install
build_script:
- npm run build
artifacts:
- path: dist\
  name: web-app